{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation-testing gate for the money-critical commerce paths. The listing escrow money code lives in its OWN file (listings-escrow.ts, extracted P2.4). The ranges below still scope to the fee/escrow/payment DECISION lines (notification-copy tails deliberately OUT), but the churn the old config suffered is gone: those ranges used to shift on every unrelated CATALOG edit because the money code shared listings.ts. Now catalog edits can't move them — only editing the escrow code itself does, and that already puts you in the mutation-gate mindset. Covers completeListingPurchase (fee math, ship-by deadline, reserved ledger), purchaseListing (fee math), shipListing capture-vs-dead-auth + captured ledger, confirmListingDelivery capture + released ledger, releaseExpiredReservation cancel/revert + cancelled ledger. Commissions money: single range (335-566). Run: npm run test:mutation. break=92; residual survivors are fail()/label copy + the un-simulatable single-threaded race guard.",
  "testRunner": "vitest",
  "vitest": { "configFile": "vitest.config.ts" },
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/lib/services/listings-escrow.ts:79-151",
    "src/lib/services/listings-escrow.ts:210-255",
    "src/lib/services/listings-escrow.ts:300-340",
    "src/lib/services/listings-escrow.ts:388-415",
    "src/lib/services/listings-escrow.ts:455-490",
    "src/lib/services/commissions.ts:372-557",
    "src/lib/services/commissions.ts:558-606",
    "src/lib/money.ts"
  ],
  "reporters": ["clear-text", "progress", "html"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "_comment2": "break lowered 92→90 after the money-math authority extraction (src/lib/money.ts): all fee/conservation math now lives in money.ts (mutation-tested here at ~94%, plus the money-boundary structural guard + 111 invariant sweep tests), so the listings-escrow / commissions service ranges are now pure ORCHESTRATION (fetch, guard, Stripe call, ledger) with defensive dead-letter branches that are correctly present but inherently lower-density. money.ts and the commissions money-core each hold ~94%; the aggregate floor of 90 catches real regressions in those while not penalising the extracted-math structure. Run: npm run test:mutation after touching fee math, escrow, money.ts, or refund/payout logic.",
  "thresholds": { "high": 99, "low": 95, "break": 90 },
  "timeoutMS": 15000,
  "concurrency": 4
}
