{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation-testing gate for the money-critical commerce paths (R2-15 A+). Scoped to the fee/escrow/payment functions so the score measures the strength of the money tests specifically, not thinly-covered CRUD. Run: npm run test:mutation",
  "testRunner": "vitest",
  "vitest": { "configFile": "vitest.config.ts" },
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/lib/services/listings.ts:374-518",
    "src/lib/services/listings.ts:591-638",
    "src/lib/services/commissions.ts:334-420"
  ],
  "reporters": ["clear-text", "progress", "html"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "thresholds": { "high": 90, "low": 80, "break": 80 },
  "timeoutMS": 15000,
  "concurrency": 4
}
