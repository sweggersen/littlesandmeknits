---
interface Props {
  params: string | null;
  label?: string;
}
const { params, label = 'Last flere' } = Astro.props;
---

{params && (
  <div class="text-center mt-8">
    <a href={`?${params}`}
      class="inline-block bg-primary text-primary-fg px-6 py-3 rounded-full text-sm font-medium hover:bg-primary-hover transition-colors">
      {label}
    </a>
  </div>
)}
