---
// Legacy/duplicate route. The canonical purchases list is /market/my-purchases
// (every in-app link + nav points there); this page was unlinked. Keep the URL
// working for old bookmarks by redirecting to the canonical page.
return Astro.redirect('/market/my-purchases', 301);
---
