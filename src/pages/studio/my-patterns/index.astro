---
// The pattern library is canonical at /profile/library (one CRUD path).
// This studio route is kept as a 301 so old links/bookmarks still resolve.
return Astro.redirect('/profile/library', 301);
---
